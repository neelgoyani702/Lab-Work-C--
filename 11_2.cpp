#include <bits/stdc++.h>
using namespace std;

int main()
{
    ofstream out("student.dat", ios::out | ios::binary);
    if (!out)
    {
        cout << "Cannot open file!" << endl;
        return 1;
    }
    float height[5];
    // out.write();
    out.close();
    return 0;
}
